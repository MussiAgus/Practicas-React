/* eslint-disable no-unused-vars */

// eslint-disable-next-line react/prop-types
export const GifGrid=({key, categoria})=>{
    return(
        <>
            <h1>{categoria}</h1>
        </>
    );
}